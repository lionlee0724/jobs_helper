# BOSS 求职副驾驶

本机 Chrome MV3 扩展：在 BOSS 直聘上做限速开聊与分级聊天跟进，数据与策略均在本地。

## Language

### 运行环

**环 A**：
职位发现与限速开聊（列表 → 匹配 → 立即沟通）。
_Avoid_: 海投环, 投递环

**环 B**：
职位线 tick 内、与开聊交错的自动聊天跟进。
_Avoid_: 内嵌跟进, job-run follow-up（对外文案）

**消息助手**：
独立于职位线的跟进入口（处理一批 / 当前会话 / 跑到清空）。
_Avoid_: 消息机器人, 副环

**FollowUpKernel**：
唯一的跟进处置模块：对已定位会话做意图识别并执行动作（发简历 / 回复 / 转人工等）。环 B 与消息助手都只调用它。
_Avoid_: processFollowUp（单指函数名时可保留）, 跟进服务

### 匹配与策略

**硬否**：
开聊前的确定性拒绝规则：期望城市、最低薪资(K)、最大年限差、排除词。字段为空表示不启用该条；证据不足时不拒绝。
_Avoid_: 本地硬过滤引擎, 前置 LLM 过滤

**显式日上限**：
用户已保存到策略中的每日开聊上限数字。仅选择风控档位而尚未保存具体数字，不构成显式日上限。
_Avoid_: 有效日上限, effective daily cap（实现细节）

**风控档**：
保守 / 平衡 / 激进等预设限速与拟真参数包；用于预填，不替代显式日上限。
_Avoid_: 风险模式, risk profile（可对内）

### 交付与债务

**Spec 债务**：
Design Spec 已要求、当前阶段明确不交付、且不得在产品文案中宣称已完成的能力。
_Avoid_: 技术债（过宽）, won't fix

**当前列表锁定**：
启动环 A 时以用户当前 BOSS 列表页为职位来源，本阶段不做推荐/期望多源自动轮询（该能力为 Spec 债务）。
_Avoid_: 职位池三源（在已交付语义下）
